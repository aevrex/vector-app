name: vector

services:
  vector:
    build: .
    image: vector:local
    container_name: vector
    ports:
      - "8080:8080"
    environment:
      DB_PATH: /data/vector.db
    volumes:
      - ./data:/data
    restart: unless-stopped
